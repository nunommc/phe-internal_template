require "spec_helper"

describe Phe::InternalTemplates do
  it "has a version number" do
    expect(Phe::InternalTemplates::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
